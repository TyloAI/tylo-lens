# Basic MCP example

This example demonstrates **MCP-style** request instrumentation.

It wraps a minimal MCP client-like object (with a `request(method, params)` function) using
`wrapMCPClient()`.

## Run

```bash
pnpm --filter @protoethik-ai/example-basic-mcp start
```

